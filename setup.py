from setuptools import find_packages, setup

setup(
    name="streamlit-example",
    version="0.1.0",
    description="Unified Streamlit + FastAPI + ML example",
    packages=find_packages(),
    include_package_data=True,
)
